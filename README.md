
  # High-Fidelity Web UI Mockup

  This is a code bundle for High-Fidelity Web UI Mockup. The original project is available at https://www.figma.com/design/Ti0IVkWTanSc0O8uS9ZpcZ/High-Fidelity-Web-UI-Mockup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  